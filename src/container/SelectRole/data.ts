import {SVGS} from '../../config';

const roles = [
  {role: 'parent', label: "I'm a Parent", Icon: SVGS.ParentRole},
  {role: 'child', label: "I'm a Child", Icon: SVGS.ChildRole},
];

export {roles};
